// Empty shim for the `server-only` Next.js guard package.
// Used by vitest so server-action modules can be imported (and then mocked)
// without the guard throwing in a jsdom environment.
export {};
